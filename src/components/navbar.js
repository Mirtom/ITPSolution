import React from 'react'
import Container from '@material-ui/core/Container';
import Grid from '@material-ui/core/Grid';

export const navbar = () => {
    return (
        <>
            <Grid item xs={12} sm={2}>
                <Container fixed primary className="nav-bar">
                
                </Container>
            </Grid>
        </>
    )
}

